const fs = require("fs");

const path = process.argv[3];
const data = process.argv[3];

/* fs.appendFile(path, `/n${data}`,{ encoding: "utf-8", (err) => 
    if(err) console.error("No se pudo editar el archivo")
    else console.log("Archivo modificado");
});
 */